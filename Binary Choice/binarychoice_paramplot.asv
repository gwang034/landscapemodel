function binarychoice_paramplot(ymintraj, ymaxtraj)

x_fun = @(y) (sqrt(2)*sqrt(y+3*y^2+6*y^3)) / sqrt(-5 + 9*y + 18*y^2)

figure()
for y=ymintraj:ymaxtraj
    x = [x_fun(y) -x_fun(y)];
    real_eval = isreal(x);
    x = real_
    p1 = 4.*x.^3 - 8.*x.*y;
    p2 = -4*y^3-3*y^2+4.*x.^2-2*y;
    plot(p1, p2, '.', 'Markersize', 20)
    hold on
    
end
title('Parameters for y = -10:10')
    





end